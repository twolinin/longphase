CC        = gcc
CXX      = g++
AR       = ar
AWK      = awk
CFLAGS   = -g -Wall -O2 -pedantic -std=c99 -D_XOPEN_SOURCE=600
CPPFLAGS = -std=c++17 -g -Wall -O3 -fopenmp
LDFLAGS  =
LIBS     =

# ─── Base objects (always built) ───
OBJ = Haplotag.o ParsingBam.o Util.o HaplotagProcess.o PhasingProcess.o \
      Phasing.o PhasingGraph.o ModCall.o ModCallParsingBam.o ModCallProcess.o \
      Compare.o CompareProcess.o main.o

# ─── ONNX Runtime auto-detection ───
# Place ONNX Runtime in the project directory:
#   cd longphase/
#   wget https://github.com/microsoft/onnxruntime/releases/download/v1.17.1/onnxruntime-linux-x64-1.17.1.tgz
#   tar xzf onnxruntime-linux-x64-1.17.1.tgz
#   mv onnxruntime-linux-x64-1.17.1 onnxruntime
#
# The Makefile will automatically detect it and enable GNN support.
ONNXRT_DIR = onnxruntime
ONNXRT_FOUND = $(wildcard $(ONNXRT_DIR)/include/onnxruntime_cxx_api.h)

ifneq ($(ONNXRT_FOUND),)
    $(info [GNN] ONNX Runtime found at $(ONNXRT_DIR))
    OBJ += GNN.o GNNProcess.o
    GNN_CPPFLAGS = -DHAS_ONNXRUNTIME -I$(ONNXRT_DIR)/include
    GNN_LDPATHS  = -L$(ONNXRT_DIR)/lib -Wl,-rpath,'$$ORIGIN/$(ONNXRT_DIR)/lib'
    GNN_LIBS     = -lonnxruntime
else
    $(info [GNN] ONNX Runtime not found at $(ONNXRT_DIR)/ — GNN module disabled)
    $(info [GNN] To enable: download ONNX Runtime to $(ONNXRT_DIR)/)
    GNN_CPPFLAGS =
    GNN_LDPATHS  =
    GNN_LIBS     =
endif

PROGRAMS = longphase

all: subprojects

subprojects:
	$(MAKE)	-C	./jemalloc
	$(MAKE)	-C	./htslib
	$(MAKE)	$(PROGRAMS)

ALL_CPPFLAGS = -I. $(CPPFLAGS) $(GNN_CPPFLAGS) -Ihtslib -Ijemalloc
ALL_LDFLAGS  = $(LDFLAGS) -Lhtslib/htslib -Ljemalloc/lib $(GNN_LDPATHS)

# Usually config.mk and config.h are generated by running configure
# or config.status, but if those aren't used create defaults here.

config.mk:
	@sed -e '/^prefix/,/^LIBS/d;s/@Hsource@//;s/@Hinstall@/#/;s#@HTSDIR@#htslib#g;s/@HTSLIB_CPPFLAGS@/-I$$(HTSDIR)/g;s/@CURSES_LIB@/-lcurses/g' config.mk.in > $@

config.h:
	echo '/* Basic config.h generated by Makefile */' > $@
	echo '#define HAVE_CURSES' >> $@
	echo '#define HAVE_CURSES_H' >> $@

include config.mk
JEMDIR = jemalloc
JEMLIB = $(JEMDIR)/lib/libjemalloc.a -ldl

$(PROGRAMS): $(OBJ)
	$(CXX) $(ALL_CPPFLAGS) $(ALL_LDFLAGS)	-o $@ $^ $(HTSLIB_LIB) $(JEMLIB) $(GNN_LIBS)

%.o: %.cpp
	$(CXX) $(ALL_CPPFLAGS) -o $@ -c $^

mostlyclean:
	-rm -f *.o

clean: mostlyclean
	-rm -f $(PROGRAMS)

distclean: clean
	-rm -f config.cache config.h config.log config.mk config.status
	-rm -f TAGS
	-rm -rf autom4te.cache

clean-jemalloc:
	$(MAKE)	-C	./jemalloc clean

distclean-jemalloc:
	$(MAKE)	-C	./jemalloc distclean

relclean-jemalloc:
	$(MAKE)	-C	./jemalloc distclean

clean-all: clean clean-htslib clean-jemalloc

distclean-all: distclean distclean-htslib distclean-jemalloc

mostlyclean-all: mostlyclean mostlyclean-htslib


tags:
	ctags -f TAGS *.[ch] misc/*.[ch]

force:

.PHONY: all subprojects
.PHONY: mostlyclean clean distclean
.PHONY: clean-jemalloc distclean-jemalloc relclean-jemalloc
.PHONY: clean-all distclean-all mostlyclean-all
.PHONY: tags force
